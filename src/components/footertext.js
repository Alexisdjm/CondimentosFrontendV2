const FooterText = ({firstPart, lastPart}) => {
  return (
    <h1 className='intro-title publicity-title'>
      {firstPart}
      <strong>{lastPart}</strong>
    </h1>
  )
}

export default FooterText